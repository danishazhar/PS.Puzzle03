The solution should work fine straight out of the box.

The laptop running the project should be connected to the internet as the angular scripts are being downloaded from the CDN's. 

It is a simple design with the focus being on my understanding of the architecture of Angular, C# and how the two bind together. There are lots of things that could be done better (like compression, minification), but for the purpose of the exercise, this should suffice and  non-compressed files are easier to understand the flow of code.

The main angular files including the directive are inside the 'PS.Puzzle03\PS.Puzzle03\App' folder